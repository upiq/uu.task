<rolemap>
  <roles>
    <role name="Project manager"/>
    <role name="Assigned party"/>
  </roles>
  <permissions>
    <!-- Annoying that an empty permissions element has to be here to avoid traceback. Role mapper should assume no permissions element means no permissions if possible. -->
  </permissions>
</rolemap>
